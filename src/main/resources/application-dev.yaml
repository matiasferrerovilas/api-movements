spring:
  datasource:
    url: jdbc:mysql://localhost:3306/expenses?createDatabaseIfNotExist=true&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true
    username: root
    password: helios
    driver-class-name: com.mysql.cj.jdbc.Driver
  rabbitmq:
    host: localhost
    port: 5672
    username: localhost
    password: localhost
  data:
    redis:
      host: localhost
      port: 6379

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:8081

logging:
  level:
    root: INFO
